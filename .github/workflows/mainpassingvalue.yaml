name: mainpassingvalues

on: push

jobs:
  call_reusable:
    uses: ./.github/workflows/readpassingvalue.yaml
    with:
      name: "Eswar Reddy Vatticharla"
      project: "Roboshop"
      component: "Dev"
      environment: "DevEnv"