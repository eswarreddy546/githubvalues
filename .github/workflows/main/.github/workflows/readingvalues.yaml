name: Reusable Workflow 2

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello vatticharla ${{ inputs.name }}"